apiVersion: v1
kind: Service
metadata:
  name: web-app-service
  annotations:
    cloud.google.com/load-balancer-type: "External"
spec:
  type: LoadBalancer
  loadBalancerIP: 34.101.226.26  # Ganti dengan IP static yang telah dicadangkan
  selector:
    app: app1
  ports:
    - protocol: TCP
      port: 9001
      targetPort: 9001
